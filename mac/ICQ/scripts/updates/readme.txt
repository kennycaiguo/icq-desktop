Instructions on creating the update to ICQ

Just call ./prepare_update.sh with two arguments: the .dmg file and the .app. Third optional argument is a resulting directory, which defaults to $PWD/update.